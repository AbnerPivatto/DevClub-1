        //Faça um programa que inicie com dois nomes, e o programa imprima na tela o seguinte dado: Olá, meu nome é (nome 1) e meu partner do CodeClub é (nome2)

        let my_name = 'Abner'
        let partner_name = "Jesus"
        let text = `Olá, meu nome é`
        let text1 = `e meu partner do DevClub é`

            console.log (text, my_name, text1, partner_name)

        //Resolução

        const firstName = 'Abner'
        const seccondName = 'Jesus'
        const names = `Olá, meu nome é ${firstName} e meu partner do DevClub é ${seccondName}`

        console.log (names)